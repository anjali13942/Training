CREATE OR REPLACE FUNCTION pg_catalog.unknownin(cstring)
 RETURNS unknown
 LANGUAGE internal
 IMMUTABLE PARALLEL SAFE STRICT
AS $function$unknownin$function$

;
ALTER function unknownin(cstring) OWNER TO postgres;
GRANT execute on function unknownin(cstring) to postgres;