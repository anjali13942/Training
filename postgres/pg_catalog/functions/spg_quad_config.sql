CREATE OR REPLACE FUNCTION pg_catalog.spg_quad_config(internal, internal)
 RETURNS void
 LANGUAGE internal
 IMMUTABLE PARALLEL SAFE STRICT
AS $function$spg_quad_config$function$

;
ALTER function spg_quad_config(internal, internal) OWNER TO postgres;
GRANT execute on function spg_quad_config(internal, internal) to postgres;